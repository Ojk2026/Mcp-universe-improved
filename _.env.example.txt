# =============================================================================
# MCP UNIVERSE — .env.example
# Kopioi tämä: cp .env.example .env
# Täytä KAIKKI arvot ennen käynnistystä
# ÄLÄ koskaan commitoi .env-tiedostoa Gitiin
# =============================================================================

# --- PostgreSQL ---
POSTGRES_DB=mcpuniverse
POSTGRES_USER=mcpuser
POSTGRES_PASSWORD=VAIHDA_TAHAN_VAHVA_SALASANA   # min. 32 merkkiä

# --- Redis ---
REDIS_PASSWORD=VAIHDA_TAHAN_VAHVA_SALASANA       # min. 32 merkkiä

# --- Nexus Gateway (Rust) ---
JWT_SECRET=VAIHDA_TAHAN_SATUNNAINEN_64_MERKKI_AVAIN
RUST_LOG=info   # debug | info | warn | error

# --- Session Manager (Flask) ---
FLASK_ENV=production
FLASK_SECRET_KEY=VAIHDA_TAHAN_VAHVA_SATUNNAINEN_AVAIN

# --- S3-yhteensopiva tallennus (valinnainen) ---
S3_BUCKET=
S3_ENDPOINT=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# --- Frontend ---
FRONTEND_PORT=8080
VITE_API_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws

# =============================================================================
# Voit generoida turvallisia satunnaisia avaimia näin:
#   openssl rand -hex 32
# =============================================================================
